#include "shell.h"

/**
 * main - Entry point for the shell program
 *
 * Description: Initializes and runs the shell loop
 * Return: 0 on success
 */
int main(void)
{
	run_shell_loop();
	return (0);
}
